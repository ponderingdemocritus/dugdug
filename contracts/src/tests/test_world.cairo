#[cfg(test)]
mod tests {
    // import world dispatcher
    // use dojo::world::{IWorldDispatcher, IWorldDispatcherTrait};
    // // import test utils
    // use dojo::utils::test::{spawn_test_world, deploy_contract};
    // // import test utils
    // use dugdug::{
    //     systems::{actions::{actions, IActionsDispatcher, IActionsDispatcherTrait}},
    //     models::{{Miner, Mine, miner, mine}},
    // };

    #[test]
    fn test_move() { // caller
    // let caller = starknet::contract_address_const::<0x0>();

    // // models
    // let mut models = array![miner::TEST_CLASS_HASH, mine::TEST_CLASS_HASH];

    // // deploy world with models
    // let world = spawn_test_world(["dugdug"].span(), models.span());

    // // deploy systems contract
    // let contract_address = world
    //     .deploy_contract('salt', actions::TEST_CLASS_HASH.try_into().unwrap());
    // let actions_system = IActionsDispatcher { contract_address };

    // world.grant_writer(dojo::utils::bytearray_hash(@"dugdug"), contract_address);
    }
}
